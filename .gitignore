# Config locale di Claude Code — non è contenuto di progetto
.claude/

# Segreti — MAI nel repo pubblico (vedi CLAUDE.md, sezione Autenticazione/profili)
.env
.env.*
*.key
*.pem
secrets.yaml
profili.yaml

# Python (backend, quando esisterà)
__pycache__/
*.pyc
.venv/
venv/

# Node (generatore statico, quando esisterà)
node_modules/
_site/
dist/

# File temporaneo di anteprima (backend/app/routers/dev_tools.py) — mai una
# ricetta vera, rigenerato ad ogni "Anteprima" nell'editor, non va committato
content/_anteprima/

# Log di sviluppo locale (server avviati in background durante i test)
*.log

# File di sistema
.DS_Store
Thumbs.db
